<Page x:Class="NorthwindCRUD.Home"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NorthwindCRUD"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="550"
      Title="Home" Background="{StaticResource negroBrocha}">

    <Grid Background="{StaticResource negroBrocha}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <WrapPanel>
            <Image Source="Images/Home_verde.png" Height="26" Margin="50, 15, 15, 15"></Image>
            <TextBlock Margin="5" VerticalAlignment="Center" Text="INICIO" FontWeight="Bold" Foreground="{StaticResource verdeBrocha}" FontSize="18" Background="Transparent"></TextBlock>
        </WrapPanel>

        <Grid Grid.Row="1" Margin="35, 0, 35, 15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <DataGrid HeadersVisibility="Column" AutoGenerateColumns="False"
                  Grid.Row="0" Margin="0,0,0,2" 
                  Style="{StaticResource CustomDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="PRODUCTOS" Binding="{Binding ProductName}" />
                    <DataGridTextColumn Header="CATEGORÍA" Binding="{Binding CategoryName}" />
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Row="1" BorderBrush="{StaticResource verdeBrocha}" BorderThickness="1">
                <DataGrid AutoGenerateColumns="False"
                      HeadersVisibility="None"
                      Style="{StaticResource CustomDataGrid}" Name="ProductosDataGrid">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding ProductName}" />
                        <DataGridTextColumn Binding="{Binding CategoryName}" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>

</Page>
